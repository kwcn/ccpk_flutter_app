import 'package:flutter/material.dart';

class MallMainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Text("This is Mall Main Page");
  }
}